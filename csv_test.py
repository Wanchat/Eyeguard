import csv

def angle_csv():
    with open('angle.csv') as f:
        data = csv.reader(f)
        for raw_angle in data:
            return raw_angle

# def reverse(lst):
#     new = []
#     count = len(lst)-1
#     while count >= 0:
#         new.append(lst[count])
#         count -= 1
#     return new
#
# angle_2 = reverse(angle_csv())

def line_angle_top(cvs_angle, point_angle):
    line_angle = 0
    i = [i for i in cvs_angle]
    if float(i[0]) < point_angle <= float(i[1]) :
        line_angle = 13.3
    elif float(i[1]) < point_angle <= float(i[2]) :
        line_angle = 13.2
    elif float(i[2]) < point_angle <= float(i[3]) :
        line_angle = 13.1
    elif float(i[3]) < point_angle <= float(i[4]) :
        line_angle = 13.0
    elif float(i[4]) < point_angle <= float(i[5]) :
        line_angle = 12.9
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
    elif float(i[1]) < point_angle <= float(i[2]) :
        line_angle = 13.1






        print("ok")
    else:
        print("no000000000")
    print(i[0])
    print(i[1])

if __name__ == '__main__':


    print(line_angle_top(angle_csv(), 3))